xdescribe('leetcode 182: description', () => {
    function func(): void {
        throw new Error('not implemented');
    }

    // select email from
    // (select p.email, count(p.email) as nums
    // from person p
    // group by p.email) as email_count
    // where nums > 1

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
