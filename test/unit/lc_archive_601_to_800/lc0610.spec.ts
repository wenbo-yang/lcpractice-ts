xdescribe('leetcode 610: is triangle', () => {
    // -- Write your PostgreSQL query statement below
    // select t.x, t.y, t.z,
    //     case 
    //         when t.x < t.y + t.z and t.y < t.x + t.z and t.z < t.x + t.y 
    //             then 'Yes'
    //         else 'No'
    //     end as triangle
    // from triangle t 
    
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
