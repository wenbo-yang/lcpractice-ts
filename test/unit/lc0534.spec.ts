xdescribe('leetcode 534: game play analysis', () => {
    // SELECT
    // player_id,
    // event_date,
    // SUM(games_played) 
    // from activity 
    // group by player_id
    // order by event_date

    
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
