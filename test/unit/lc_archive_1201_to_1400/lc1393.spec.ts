xdescribe('leetcode 1393: capital gain loss', () => {
// SELECT
//   stock_name,
//   SUM(IF(operation = 'Buy', -price, price)) AS capital_gain_loss
// FROM Stocks
// GROUP BY 1;


    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
