xdescribe('leetcode 1350: students with invalid departments', () => {
// SELECT id, name
// FROM Students
// WHERE department_id NOT IN (SELECT id FROM Departments);
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
