xdescribe('leetcode 196: delete dupe emails', () => {
    function func(): void {
        throw new Error('not implemented');
    }

    // delete from person p1
    // where id in (select p2.id from person p2
    // join person p3 on p3.email = p2.email
    // and p2.id > p3.id)

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
