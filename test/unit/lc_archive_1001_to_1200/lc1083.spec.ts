xdescribe('leetcode 1083: sale analysis 2', () => {
    // SELECT buyer_id
    // FROM
    // Sales
    //     JOIN Product USING (product_id)
    // GROUP BY 1
    // HAVING SUM(product_name = 'S8') > 0 AND SUM(product_name = 'iPhone') = 0;
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
