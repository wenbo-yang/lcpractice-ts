xdescribe('leetcode 1108: defanging an ip address', () => {
    function defangIPaddr(address: string): string {
        return address.split('.').join('[.]');
    };

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
