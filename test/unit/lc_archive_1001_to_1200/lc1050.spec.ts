xdescribe('leetcode 1050: actors and directors who cooperated at least three times', () => {
    // SELECT actor_id, director_id
    // FROM ActorDirector
    // GROUP BY 1, 2
    // HAVING COUNT(1) >= 3;
    
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
