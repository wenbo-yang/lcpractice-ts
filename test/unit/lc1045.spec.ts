xdescribe('leetcode 1045: description', () => {
    // SELECT customer_id
    // FROM Customer
    // GROUP BY 1
    // HAVING COUNT(DISTINCT product_key) = (
    //     SELECT COUNT(*) FROM Product
    // );


    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
