xdescribe('leetcode 212: word search in a grid', () => {
    // idea:
    // revert the words
    // parse into prefix tree so we can search for them later
    //
    // start with any coordinate, do dfs capped by depth of Max(word.length)
    // loop through

    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
