xdescribe('leetcode 619: postgres', () => {
    // -- Write your PostgreSQL query statement below
    // WITH cte AS (
    //     SELECT num
    //     FROM MyNumbers
    //     GROUP BY num
    //     HAVING COUNT(*) = 1
    //     ORDER BY num DESC
    //     LIMIT 1
    // )
    
    // SELECT MAX(num) as num
    // FROM (
    //     SELECT num FROM cte
    //     UNION ALL
    //     SELECT NULL
    // ) t;
    

    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
