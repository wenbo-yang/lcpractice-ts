xdescribe('leetcode 1082: sales analysis', () => {
    // SELECT seller_id
    // FROM Sales
    // GROUP BY seller_id
    // HAVING
    // SUM(price) >= ALL (
    //     SELECT SUM(price)
    //     FROM Sales
    //     GROUP BY seller_id
    // );

    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
