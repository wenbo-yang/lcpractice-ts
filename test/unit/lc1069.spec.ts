xdescribe('leetcode 1069: product sales analysis II', () => {
    // SELECT product_id, SUM(quantity) AS total_quantity
    // FROM Sales
    // GROUP BY 1;
    
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
