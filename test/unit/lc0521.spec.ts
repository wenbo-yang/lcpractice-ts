xdescribe('leetcode 521: longest uncommon sequence', () => {
    function findLUSlength(a: string, b: string): number {
        return a == b ? -1 : Math.max(a.length, b.length);
    };

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
