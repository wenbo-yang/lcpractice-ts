xdescribe('leetcode 29: divid two numbers', () => {
    function divide(dividend: number, divisor: number): number {
        throw new Error('not implemented');
    }

    it('test case 1 Input: dividend = 10, divisor = 3, output 3', () => {
        const output = divide(10, 3);
        expect(output).toEqual(3);
    });
});
