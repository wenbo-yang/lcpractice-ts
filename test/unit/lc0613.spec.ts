xdescribe('leetcode 613: shortest distance in a point', () => {
    // SELECT MIN(p2.x - p1.x) AS shortest
    // FROM
    //     Point AS p1
    //     JOIN Point AS p2 ON p1.x < p2.x;

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
