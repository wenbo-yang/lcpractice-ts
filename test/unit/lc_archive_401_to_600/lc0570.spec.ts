xdescribe('leetcode 570: 5 direct reports', () => {
    // SELECT e1.Name
    // FROM Employee e1
    // JOIN (
    // 	SELECT ManagerId
    // 	FROM Employee
    // 	GROUP BY ManagerId
    // 	HAVING COUNT(ManagerId) >= 5
    // ) e2
    // ON e1.Id = e2.ManagerId;

    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
