xdescribe('leetcode 574: winning candidate', () => {
    // SELECT name
    // FROM
    //     Candidate AS c
    //     LEFT JOIN Vote AS v ON c.id = v.candidateId
    // GROUP BY c.id
    // ORDER BY COUNT(1) DESC
    // LIMIT 1;


    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
