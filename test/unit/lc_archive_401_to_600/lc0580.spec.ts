xdescribe('leetcode 580: description', () => {
    // SELECT dept_name, COUNT(student_id) AS student_number
    // FROM
    //     Department
    //     LEFT JOIN Student USING (dept_id)
    // GROUP BY dept_id
    // ORDER BY 2 DESC, 1;
    
    function func(): void {
        throw new Error('not implemented');
    }

    it('test case 1 Input:, target = 5, output 2 ', () => {});
});
